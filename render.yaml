services:
  - type: web
    name: andinoh-hotel-webapp
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://hotel-api-gateway.onrender.com/api/v1
      - key: NODE_VERSION
        value: 20
